import 'package:flutter/material.dart';

class ColorManager {
  const ColorManager._();

  // TODO: Color primary
  static const primaryColorApp = Color(0xff5B6CFF);

  // TODO: Color background
  // primary
  static const backgroundPrimaryColor = Color(0xffF8FAFC);

  // secondary as background for app ,  or for selected card
  static const backgroundSecondaryColor = Color(0xffEFF1FF);

  static const backgroundDisable = Color(0xffC7D2FE);

  // TODO: Color text
  // primary
  static const textPrimaryColor = Color(
    0xff1A2C62,
  ); // as a selected title form , title view

  // hint
  static const textHintColor = Color(
    0xff64748B,
  ); // as a hint in form , subTitle

  // secondary color text as a primary color app

  // default
  static const textDarkColor = Color(0xff1E293B); //   // default

  // TODO: Color Status
  // red
  static const statusRedColor = Color(0xffEF4444);

  //  orange
  static const statusOrangeColor = Color(0xffFB923C);
  static const statusBackgroundOrangeColor = Color(0xffF5E2A9);

  // blue
  static const statusBlueColor = Color(0xff6380D7);
  static const statusBackgroundBlueColor = Color(0xffE2E8F0);

  // green
  static const statusGreenColor = Color(0xff22C55E);
  static const statusBackgroundGreenColor = Color(0xffA5F7C3);
}
